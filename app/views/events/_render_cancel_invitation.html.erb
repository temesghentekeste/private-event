  <%= button_to('Cancel Invitation', invitation_path(id: id, attended_event_id: attended_event_id, event_attendee_id: current_user.id), method: :delete, class: 'btn btn-danger btn-block font-weight-bolder ', title: 'Sign In') %>